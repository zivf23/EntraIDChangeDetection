# This Dockerfile is for the DEVELOPMENT environment ONLY
# It runs the React development server with hot-reloading.
FROM node:18-alpine

WORKDIR /app

# Copy dependency definitions
# This is done first to leverage Docker's build cache
COPY package.json package-lock.json ./

# Install dependencies
RUN npm install

# Copy the rest of the application source code
COPY . .

# The React development server will run on port 3000 by default
EXPOSE 3000

# Start the React development server
CMD ["npm", "start"]